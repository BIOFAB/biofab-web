

<% @designs.each do |design| %>

  <%= render :partial => 'eou', :locals => {:biofab_id => design.strain_biofab_id, :reliability => design.reliability, :performance => design.performance_normalized, :performance_deviation => design.performance_sd_normalized, :promoter => design.promoter, :fpu => design.fpu, :promoter_performance => 0.3, :fpu_performance => 0.7} %>

<% end %>
